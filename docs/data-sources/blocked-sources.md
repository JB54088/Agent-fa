# 禁止自动采集来源

当前没有在未完成实际核验的情况下将任何单位标记为永久禁止来源。

运行时如果出现以下情况，单次任务立即标记为 `BLOCKED` 或 `NEEDS_REVIEW`，不得持续重试：

- HTTP 403；
- HTTP 429；
- 登录墙、验证码或安全验证；
- 明确禁止自动访问的robots.txt或服务条款；
- 需要破解签名、模拟登录、代理轮换或绕过Cloudflare等保护；
- 不是招聘单位官方域名的第三方聚合平台。

禁止从牛客、国聘、粉笔等第三方平台批量复制完整招聘数据库。第三方线索如确需进入平台，只能由管理员人工核验后录入，并标记为D级来源。
