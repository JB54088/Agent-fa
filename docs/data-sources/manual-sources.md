# 人工维护来源规则

以下情形默认使用 `MANUAL_ONLY` 或 `ATTACHMENT_ONLY`：

- 招聘入口需要登录；
- 页面出现验证码、风控验证或明确访问限制；
- robots.txt或服务条款不允许自动访问；
- 页面只能在移动端或内部接口中访问；
- 官方身份无法确认；
- JavaScript页面无法在不绕过保护的情况下稳定读取；
- 扫描版PDF无法可靠提取文本；
- 招聘内容需要人工判断专业、时间或资格条件。

人工录入仍必须保存原始公告链接、来源名称、原文摘录、采集时间和管理员审核记录。人工上传的PDF、Excel、CSV也必须先进入原始数据和审核队列。
